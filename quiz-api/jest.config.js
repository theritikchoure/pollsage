module.exports = {
    testEnvironment: 'node', // Use the Node.js environment for testing
    roots: ['<rootDir>/__tests__'], // Define the root directory for tests
    testMatch: ['**/*.test.js'], // Define the pattern for test files
  };
  